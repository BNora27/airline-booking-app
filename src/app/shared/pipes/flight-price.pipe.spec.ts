import { FlightPricePipe } from './flight-price.pipe';

describe('FlightPricePipe', () => {
  it('create an instance', () => {
    const pipe = new FlightPricePipe();
    expect(pipe).toBeTruthy();
  });
});
